[project]
name = "custom-component-module"
version = "0.1.0"
description = "Python module containing components"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pulumi",
    "pulumi-aws",
    "pydantic>=2.11.9",
]

[build-system]
requires = ["uv_build"]
build-backend = "uv_build"
